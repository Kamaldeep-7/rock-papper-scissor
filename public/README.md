# public/

Drop the built `rps.apk` here as `public/rps.apk`.

The Node server (`server/index.js`) serves this folder at `/downloads/*`,
so the URL the Menu's download button uses is `/downloads/rps.apk`.

See `mobile/README.md` for build instructions.
